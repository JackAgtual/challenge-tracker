spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-bwiy8c41xjixmzmy.us.auth0.com/

auth0:
  audience: https://challenge-tracker-api
